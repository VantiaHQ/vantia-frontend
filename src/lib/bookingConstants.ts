/**
 * Valores sin env, seguros de importar en componentes cliente.
 * Mantener alineado con la lógica de reservas en servidor (`bookingConfig`).
 */
export const BOOKING_MAX_DAYS_AHEAD = 60;
export const BOOKING_LEAD_TIME_MINUTES = 60;
